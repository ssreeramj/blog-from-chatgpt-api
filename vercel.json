{
    "public": false,
    "devCommand": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "builds": [
        {
            "src": "redrock/main.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/redrock/main.py"
        }
    ]
}